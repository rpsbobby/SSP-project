<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="UTF-8" />
      <meta http-equiv="X-UA-Compatible" content="IE=edge" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <link
         href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
         rel="stylesheet"
         integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
         crossorigin="anonymous"
      />
      <script defer src="add.js"></script>

      <title>Zoobook | Add new</title>
   </head>
   <body class="d-flex flex-column min-vh-100">
      <div class="bg-light py-4 border-bottom border-primary">
         <nav class="nav nav-pills nav-justified container">
            <a href="/" class="nav-link h3 border border-primary">Main</a>
            <a href="#" class="nav-link h3 active">Add new</a>
         </nav>
      </div>

      <div class="container py-5">
         <h3 class="display text-center border-bottom py-3">Add your pet</h3>
         <form action="/add" method="post" id="form">
            <div class="mb-3">
               <label class="form-label">Animal's name</label>
               <input
                  class="form-control"
                  type="text"
                  placeholder="name"
                  name="name"
                  id="name"
               />
            </div>
            <div class="mb-3">
               <label class="form-label">Photo's URL</label>
               <input
                  class="form-control"
                  type="text"
                  placeholder="url"
                  name="url"
                  id="url"
               />
            </div>
            <div class="mb-3">
               <label class="form-label">Description/Fun Fact (Optional)</label>
               <input
                  class="form-control"
                  type="textarea"
                  placeholder="..."
                  name="fact"
                  id="textField"
               />
            </div>
            <div class="mb-3 d-flex justify-content-around">
               <button class="btn btn-primary" type="submit">Add</button>
               <div class="btn btn-primary" id="randomise">Randomise</div>
            </div>
         </form>
      </div>
      <footer class="bg-primary text-white pt-5 pb-4 mt-auto">
         <div class="container text-center">
            <h3>Robert Szlufik</h3>
            <h4>Student #2020358</h4>
         </div>
      </footer>
   </body>
</html>
