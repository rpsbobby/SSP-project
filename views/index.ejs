<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="UTF-8" />
      <meta http-equiv="X-UA-Compatible" content="IE=edge" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <link
         href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
         rel="stylesheet"
         integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
         crossorigin="anonymous"
      />
      <title>Zoobook</title>
   </head>
   <body class="d-flex flex-column min-vh-100">
      <!-- navbar -->
      <div class="bg-light py-4 border-bottom border-primary">
         <nav class="nav nav-pills nav-justified container">
            <a href="#" class="nav-link active h3">Main</a>
            <a href="/add" class="nav-link h3 border border-primary">Add new</a>
         </nav>
      </div>
      <!-- main section -->
      <div class="container py-4 d-flex justify-content-around flex-wrap">
         <!-- card here  -->
         <% if(arr.length===0) { %>
         <h1>No Animals added!</h1>
         <% } else { %> <% arr.forEach((animal) => { %>
         <div class="card m-4" style="width: 18rem">
            <img
               src="<%=animal.imageURL%>"
               class="card-img-top"
               style="min-width: 18rem; max-width: 18rem; max-height: 18rem"
               alt="..."
            />
            <div class="card-body text-center">
               <h5 class="card-title"><%=animal.name%></h5>
               <p class="card-text"><%=animal.fact%></p>
               <form action="/delete" method="post">
                  <input type="hidden" value="<%= animal.id %>" name="id" />
                  <button class="btn btn-primary" type="submit">Delete</button>
               </form>
            </div>
         </div>
         <% }) %> <% } %>
      </div>
      <!-- footer -->
      <footer class="bg-primary text-white pt-4 pb-4 mt-auto">
         <div class="container text-center">
            <h3>Robert Szlufik</h3>
            <h4>Student #2020358</h4>
         </div>
      </footer>
   </body>
</html>
